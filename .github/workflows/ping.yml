name: Render Ping

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping website
        run: curl -s -o /dev/null -w "%{http_code}" https://full-stack-mern-project-roan.vercel.app/